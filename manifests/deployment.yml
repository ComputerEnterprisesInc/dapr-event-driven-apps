apiVersion : apps/v1
kind: Deployment
metadata:
  name: "daprtalkdemo-38ab"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "daprtalkdemo-38ab"
  template:
    metadata:
      labels:
        app: "daprtalkdemo-38ab"
    spec:
      containers:
        - name: "daprtalkdemo-38ab"
          image: "daprtalk.azurecr.io/daprtalkdemo"
          ports:
          - containerPort: 5000